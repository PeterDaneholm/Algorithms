namespace Algorithms;

public class HashMaps
{
    //Hash Map
    /*
     * A hash map is a version of a hash table that will usually hold a large amount of data
     * It will hold a reference with a Key-value pair to entries in the buckets. The key is unique for each entry
     * The hash code is generated from the 'Key' part of the pair, to determine which bucket it should be placed in
     * 
     */
}